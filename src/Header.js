const Header = () => {
    return (
        <div className="className">
            <a href="#">HOME</a> -
            <a href="#">FEED</a> -
            <a href="#">MESSAGES</a> -
        </div>
    );
}

export default Header;